# turn on Alfred after sunrise
alias: Good morning, Alfred
trigger:
  platform: sun
  event: sunrise
  offset: '1:00:00'
condition:
  - condition: state
    entity_id: switch.alfred_outlet
    state: 'off'
action:
  - service: switch.turn_on
    entity_id: switch.alfred_outlet
